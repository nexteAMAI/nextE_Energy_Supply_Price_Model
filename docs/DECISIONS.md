# DECISIONS

Decision numbers continue the D-series of the Reference Case workbook's `_CLAUDE_LOG`
(D01-D86 in the workbook; D87/D88 are the `Pricing_Calc` corrections applied in code, see
OPEN_ITEMS.md). Gate rulings are referenced by their G-ids from the Phase 0 specification.

| ID | Date | Decision | Basis |
|---|---|---|---|
| G0-D2 | 14.09.2026 | The engine grid is positional (96 rows per day, EET labels, fixed 1 h offset to CET); daylight-saving rules are applied at import; the Reference Case series are frozen as they sit | CEO ruling on X-02 |
| G0-D4 | 14.09.2026 | Numerical tolerance `abs(py - xl) <= 1e-6 x max(abs(xl), 1)` | CEO ruling |
| G0-D6 | 14.09.2026 | Fixed evaluation order with an acyclicity assertion; no convergence iteration | CEO ruling on X-14 |
| G0-D7 | 14.09.2026 | Reference Case keeps the placeholder off-takers (positions 3 and 4) Active | CEO ruling |
| G0-F035 | 14.09.2026 | No counterparty names in the repository; entities are coded (`OT1`.. by merit-order position, `PV1`); names live in the application and in Excel files | CEO ruling |
| G0-PAR | 14.09.2026 | All parameters, including off-taker names and count, are dynamic and set in the application; YAML registers carry Reference Case values only | CEO ruling |
| G0-TS | 14.09.2026 | All time series are uploaded through the application against the standard template of docs/DATA_CONTRACT.md | CEO ruling |
| D89 | 14.09.2026 | The 35.136-row workbook container is not reproduced: the engine grid has 365 x 96 rows in a common year (366 x 96 in a leap year); placeholder rows (29.02 in v03, `Outside` rows in the STD template) are dropped | every workbook aggregation is date-keyed; the two containers place the placeholder block differently (X-29) |
| D90 | 14.09.2026 | Upload contract carries `time_basis` (`local_clock` / `fixed_96`) in `Std_Control` and `k` per volume slot in `Series_Registry`; both are declared, never inferred | SPEC section 4.3, X-24 |
| D91-ESB | 14.09.2026 | Fixture workbooks are written by openpyxl (16 significant digits); the parquet built from the workbook's stored text is the canonical fixture | build_rc_fixtures.py |
| D87 | 14.09.2026 | `Pricing_Calc!C46` (forecast premium) is the off-taker's total forecast premium; the workbook's +94 row offset (Collateral component) is a defect reproduced only under `pricing_as_cached=True` for the tie-out | X-15; `esb.pricing` |
| D88 | 14.09.2026 | `Pricing_Calc!C39` (cost to serve) is `(portfolio OPEX + variable OPEX + own BGL fee + memo BGL share + memo interest share) / metered`; the workbook's retired labels return 0 and are reproduced only under `pricing_as_cached=True` | X-16; `esb.pricing` |
| D92 | 14.09.2026 | Grid charges (TSO and DSO) settle on the TSO payment terms, as the workbook links `Input!C23` to `G80` | `esb.cashflow.build_cashflow`; tie-out of `CF_Mth` |
| D93 | 14.09.2026 | The parity fixture `rc_v03_expected.parquet` holds every cached numeric cell of the five result sheets; 153 layout cells (label self-check column, anchors, blank manual column) are declared not applicable and listed in the parity report rather than mapped | `esb.parity`; `tests/parity` |
| D94 | 14.09.2026 | The engine computes the price build-up for every off-taker; only the position-3 view is cached in the workbook and can be tied. The other views are validated by the same code path and the module tests | `esb.pricing` |
| G3 / G4 | 14.09.2026 | Gates G3 (edge suites) and G4 (parity tie-out, corrected `Pricing_Calc` values D87 / D88) accepted by the CEO | CEO ruling | — |
| PSTORE | 14.09.2026 | Run-time parameters persist in a **versioned scenario file** (`ESB-SCN 1.0`, JSON: name, integer version, timestamp, engine version, md5 of the register, history, the full register incl. display names); no database in this phase. The file lives outside the repository; names are allowed in it (F-035) | CEO ruling | Closes OPEN_ITEMS PSTORE |
| D95 | 14.09.2026 | Display names of off-takers and counterparties are fields of the register (`name`, empty in the repository); the application shows the name when set and the code otherwise. The engine never reads a name | F-035; PSTORE | — |
| D96 | 14.09.2026 | Uploads are joined by `esb.assemble`: the Reference Case fixture is an optional base layer, each accepted delivery replaces its series, a wholesale delivery is keyed to a scenario by the application's choice or the `Std_Control` label; an Active off-taker or source without a complete series, or an active scenario without prices, blocks the run with a named message. Blank stays blank | rules 2, 3 and 6 | — |
| D97 | 14.09.2026 | The case bundle (`ESB-CASE 1.0`, zip) carries the scenario file, every accepted upload byte-identical, the importer provenance and the headline results; re-opening re-imports through the same checks, re-runs, and compares the headline values at the parity tolerance; a member with a changed md5 is refused | prompt section 8.3 | — |
| D98 | 14.09.2026 | The application never shows a traceback: importer refusals, coverage problems and engine errors are shown as named messages; the session log records every upload, refusal, change, run and export | rule 6 | — |
| D99 | 14.09.2026 | Baseload nomination uploads are accepted and listed but not consumed: the engine sources Baseload from the strips of the register, as the Reference Case does | parity first | Phase 7 may consume them |
| D100 | 14.09.2026 | "Iteration count" on the cash-flow page is shown as the calculation-order trace with the statement that the count is 0 by construction | G0-D6 | — |
| D101 | 14.09.2026 | The retail NM pre-tax KPI target (3,00 EUR/MWh) is a register value (`meta.kpi_retail_nm_target_eur_per_mwh`), editable on the Parameters page | prompt section 10.1 page 10; house benchmark EW-NFR-01 | — |
| D102 | 14.09.2026 | Deployed on Streamlit Community Cloud, account `nexteamai`, as `nexte-esb.streamlit.app` (`main`, `app.py`, Python 3.12). No earlier ESB app existed on the account; the URL in the legacy user guide is retired. The account's single private-app slot is held by the PMO tool, so the ESB app is a public link protected by an in-app sign-in gate whose credentials live only in the cloud secrets; the production access model is decided at the production stage | CEO ruling 14.09.2026 (option 1) | — |
| D103 | 14.09.2026 | A credential that has passed through a chat or e-mail is treated as exposed and rotated in the Secrets box; the application never logs what is typed at sign-in | credentials rule | — |
| D104 | 15.09.2026 | Brand CSS hides only the Deploy button and the main menu; the sidebar expand control stays visible (hotfix 0.5.1 - the collapsed sidebar could not be reopened on the live app) | G5 finding | — |
| D105 | 15.09.2026 | Every metric label carries its leg as a suffix (`esb.labels.leg_of`): **Retail**, **Wholesale spot resell** or **Total**; the leg word is not repeated in the label text (`t_gm2_forecast` -> "GM2 forecasted · Total"); off-taker sections and the pricing sheet are Retail by construction, the cash-flow tables Total; checks, market series, source availability before allocation and parameters carry no leg. Applies to KPI cards, tables, chart series, block names and the Excel export | CEO walk-through request C1, 15.09.2026 | — |
| D106 | 15.09.2026 | Numeric entry is Romanian on every surface: text-based fields and grids show 1.234,56 and parse it back (dot = thousands, comma = decimal; a single dot with other than three digits after it is a decimal point; a non-number or a value outside its bounds is refused in place and the previous value kept); integer fields (days, year) stay numeric steppers. Plotly axes use `separators=",."`; log text, scenario timestamps and export dates use dd.mm.yyyy | CEO walk-through request C2, 15.09.2026 | — |
| D107 | 15.09.2026 | An Inactive off-taker without series contributes zeros to the merit order (an added position can be saved and run before its delivery arrives); an Active one without series still blocks the run (D96). The full quarter-hour frame and the `QH_full` export drop pre-existing `date`/`interval` columns before inserting the grid's | G5 walk-through defects | — |
| D108 | 15.09.2026 | Input templates and exports share the brand styling (Montserrat 9 pt, navy header, paper input fill, locale-aware number tokens `#,##0.000` / `#,##0.00` so a Romanian Excel renders 1.234,50); the canonical templates the CEO formats are kept in the CEO folder `00_Spec/01_current/{input,output}_data_template/` and, once approved, will be served by the application from `data/templates/` (Phase 7) | CEO request B, 15.09.2026 | Phase 7 |
| D109 | 15.09.2026 | Regulated tariff components per off-taker are derived from a grid tariff table (`config/tariffs_ro.yaml`, RON/MWh by charge owner and component, taken from sheet `Grid_Taxes_RO` of the v1.1 template, validity 2026, `source_status: unverified`) when the off-taker names its distribution operator and metering-point voltage level; the cascading rule of the v1.1 template applies (TL, TG, SS, cogeneration, CfD, excise at every level; T_HV from HV DSO down, T_MV from MV DSO down, T_LV at LV DSO only); EUR at the register FX; GC stays the register derivation. DEER at MV DSO reproduces the Reference Case set exactly. An operator without distribution rows (Retele Electrice Romania in the source sheet) is refused, not zeroed. The table is editable in the admin tab and travels in the scenario file; the portfolio set and the manual override remain available | CEO request 15.09.2026 (3); v1.1 `Dashboard_Input` rows 301-314 | X-03 narrowed |
| D110 | 15.09.2026 | The fixed guarantee amount of every counterparty is a user input; for the PV source the workbook derivation of `Input!C85` (mean PV cost budget + mean PV resell cost) is the default shown and used only while the register holds no value (the Reference Case register, so parity is unchanged). The "derived by the engine" switch is removed | CEO request 15.09.2026 (4) | — |
| D111 | 15.09.2026 | Every table of the application shows the unit of measurement of every number: a Unit column after the line label for line-oriented tables (`esb.labels.unit_of`), the unit in the column header for column-oriented tables; table rows are white (no alternating fill) | CEO requests 15.09.2026 (1), (2) | — |
| D112 | 16.09.2026 | Upload contract **ESB-STD-QH 1.1** (backwards compatible with 1.0): optional registry columns `scenario_name` (wholesale files may carry several scenario blocks; series are keyed `scenario_name + name`, frames `<scenario>__<name>`) and `entity_name` (reference only, never read into the engine); a declared slot whose RAW column is entirely blank is **not delivered** (listed in the provenance, dropped from the registry and the frame) - a partly blank column is still a gap and is refused; slot ids may have any digit width; templates are written as 1.1 and sized to the register (entities, scenarios) | Template audit 16.09.2026 (rulings D-A, D-B); D-TPL | 1.0 files keep the old behaviour |
| D113 | 16.09.2026 | **Canonical output workbook (TPL-CANON).** The CEO's formatted template (`TPL_output_run_export_ESB.xlsx`, CEO folder) is the visual reference; `tools/extract_layout.py` extracts its structure into `data/layout/run_export.json` (band, header, widths, every row with engine key / role / unit, block rhythm, grid blocks) and `esb.layout` renders every export from that specification with the run's values. The standard `nexte-output-workbook-formatting.md` lives in the renderer as code: palette roles (data `#EEF3FA`, total / column header `#0E1C2E`, section `#1F3E66`, group `#F4F4F4`, index `#F7F6F3`, memo italic muted), Montserrat type scale (12 data, 18 block titles, 16 sheet title, 11 banner, 10 band), number-format catalogue with role decimals (monthly volumes 0 dp, daily 3 dp, quarter-hour 4 dp; MW 1 / 3 dp; prices 2 dp, KPI price rows 3 dp; shares 1 dp; checks 6 dp; `dd"."mm"."yyyy`), metadata band rows 1-4 with A4 the application's stamp, header row 6, Unit column B, spacer columns, blank separators, hairline borders on content only, gridlines off, no merges (Center Across Selection), no formulas, freeze at the header row plus label and unit columns. Units come from `esb.labels.unit_of`, never from the template (the template's unit-cell errors cannot recur); the off-taker block of `Cons_P&L` is one template replicated per off-taker with the display name in the section header; `CF_Daily_Ledger` is a 366-day frame with the Summary block below; `Parameters` carries Unit / Standard-default / Source-vintage columns (catalogue to be completed under T12.7); `Provenance` follows the 11-column schema with `Requested by` = the signed-in user. The CEO's template files (5-88 MB) never enter the repository | Template audit 16.09.2026 (rulings D-C .. D-I as recommended); CEO side note 1 | Rulings D-D (correction of the template files) and D-F / D-G (QH_daily key row, CSV order) remain with the CEO |
| D114 | 16.09.2026 | The wide grids (`QH_full`, `QH_daily`) carry the template's calendar block first (EET and CET twins of year, month, week, date, day, weekday, season, day-of-year, hour, interval, the weekend / Romanian public-holiday flag, the DAM peak flag and the interval start / end times), then the sequence, then the engine keys by block under Center-Across-Selection block titles with KPI rows Total / Average (<>0) / Maximum / Minimum, a share row, the engine-key row, the unit row and the wrapped parameter-name row. Public holidays are rules in `config/calendar_ro.yaml` (fixed dates plus the movable feasts from the computed Orthodox Easter), `source_status: to_verify`. The template's duplicate flag column (O-19) is not reproduced. Data rows of the grids are streamed as sheet XML with the probe row's style ids (23 s and 54 MB for the full frame instead of 560 s) | Template audit (O-19); performance | Calendar field interpretations O-20 to be confirmed |
| D115 | 16.09.2026 | The application's tables adopt the workbook roles and role decimals (D-H): `roles` per row (total, subtotal, memo, check, section) change weight, rule and tone only - rows stay white (D111); decimals follow the unit and the table's granularity (`app.brand.UNIT_DECIMALS`, the catalogue of D113); the pricing and overview pages pass the unit context (`unit_of(key, ctx)`) so a pass-through is EUR/MWh on the pricing sheet and EUR in the overview | CEO side note 1 (consistency across output layers) | — |
| D116 | 17.09.2026 | **CEO rulings on the template audit (`D022`) and the resume-prompt topics:** D-D (a) executed — corrected copies of the six template files (streaming XML: unit cells, `NM · Total`, `check`, name inside the QH label renamed, merges → Center Across Selection, `hh:mm`, scroll reset, `entity_name`, Claude Log sheets removed), originals superseded, the layout spec re-extracted from the corrected file; D-G (a) — one QH column order everywhere (calendar block, sequence, engine keys by block; the CSV carries the engine keys as header in that order; the CSV layout template retired); D-I (a)+(b) — `config/parameter_catalogue.yaml` (unit by rule, explicit standard / source / source status) rendered on the Parameters sheet and the Audit page now, sources completed by the regulatory verification pass; D-A/B/C/E/F/H confirmed; calendar interpretations (O-20) confirmed as coded, holiday list verified in the regulatory pass (HOL-RO); regulatory verification pass ordered (X-05, TAR-SRC); G5 taken page by page by the CEO; Phase 7 = all candidates in the recommended order, one shared user for now; access model (d) self-hosting on the `nexte` VM behind the tailnet; G2 accepted conditionally; the supply-contract carry-overs in a dedicated session | CEO, 17.09.2026 | — |
| D117 | 17.09.2026 | **Compliance check of the live v0.7.0 outputs** (`OUTPUT_COMPLIANCE_GW-ESB-01_17092026.md`): two high findings fixed in 0.7.1 — W-1 a counterparty name inside a QH label (also in `run_export.json`, commit `31d30e9`; history rewrite is the CEO's call), W-6 `Portf Overview` row 81 `NM` resolved to the resell leg (the extractor now prefers the Total leg for an untagged measure and refuses ambiguous labels; regression test). Also: Parameters dates as real dates, unit-bearing formats from the catalogue, CSVs with key / label / unit and decimals by unit (`csv_rows`), column CSVs with keys as header and `dd.mm.yyyy` (`csv_columns`), negative zeros normalised, no default date format registered | compliance check | — |
| D118 | 17.09.2026 | **Regulatory verification pass (CEO ruling "regulatory constants and tariffs (a)"): one pass consuming CW-EMK-01 and the primary sources on line, results into the parameter catalogue with sources, validity periods and check dates.** Verified on the primary source: VAT 21 % (Codul fiscal art. 291 alin. (1), Legea nr. 141/2025, from 01.08.2025), CIT 16 % (art. 17 - the workbook's "art. 41" is the payment article), reverse charge art. 331 alin. (2) lit. e) with its sunset 31.12.2026 (alin. (6)), GC quota 2026 0,499387 (ANRE Ordinul nr. 81/2025), GC minimum value 2026 148,2201 RON/GC (Legea nr. 220/2008 art. 11; OPCOM 08.01.2026), Vtm = 2 (PO TEL 01.13 pct. 8.2.1), Vdm = 1 + overdue add-on (ANRE Ordinul nr. 129/2015 art. 8), TG 3,63 / SS 14,70 (ANRE Ordinele nr. 74 / 73/2025). Contradicted and kept for parity pending CEO rulings: the BRP rule 9.000 RON/MW (no such rule in PO TEL 00.45 - BRP-GF), TL 36,54 for 36,45 and the distribution set (applied tariffs of Distributie Oltenia under DEER's name, summed by the cascade - TAR-2026). Status vocabulary of the catalogue: verified, verified_secondary, contradicted, unverified, to_verify, not_published, assumption; a `contradicted` entry names its open item. The verified specific tariffs of ANRE Ordinele nr. 73-78/2025 are shipped as `config/tariffs_ro_2026_anre.yaml`, not loaded. The CW-EMK-01 note A1 is annotated (VAT 19 % stale) | CEO ruling 17.09.2026; primary sources listed in `config/parameter_catalogue.yaml` | X-05, TAR-SRC, HOL-RO closed; TAR-2026, BRP-GF, RC-2027, GC-2027, TAR-2027 opened |
| D119 | 17.09.2026 | **CEO rulings on the verification findings.** TAR-2026 (a): the verified specific tariffs of ANRE Ordinele nr. 73-78/2025 are the application's grid tariff table (`config/tariffs_ro.yaml`, `basis: specific`; the v1.1 table kept as `config/tariffs_ro_v11_template_superseded.yaml`); the cascade sums them to the applied tariff of the connection level; the frozen Reference Case register keeps its explicit components (parity untouched); scenarios saved with the old table are re-based with the reset button on the Parameters page. BRP-GF: nextE delegates its balancing responsibility to a PRE service provider (CINTA), so the Transelectrica PRE guarantee procedure does not apply to nextE directly; the engine gains the method `pre_delegated` - guarantee = max(initial guarantee of the service contract, months x average monthly imbalance value of the engine's own ledger x (1 + VAT)) with register fields `pre_initial_ron` (100.000 RON, art. 9.8 of the CINTA template contract read on the signed nextE Asset contract of 29-30.01.2026), `pre_months_of_imbalance` (2, mid of the 1-3 band of the CINTA PRE procedure pct. 5.2.5, assumption) and `pre_vat_inclusive` (assumption); the workbook rule stays the Reference Case method for parity. Also in that contract: a fixed monthly fee plus 5 % of the aggregation gain (Anexa 2), payment by the member within 4 working days of receipt (procedure pct. 5.2.2 - the register's BRP payment terms of 15 days are the workbook's, to be aligned in the bid scenarios), and a joint-and-several contribution to CINTA's own Transelectrica guarantee pro rata to the members' imbalance values (Anexa 1 A1.2.3 - not modelled, flagged). RC-2027 as recommended: bid scenarios for a spine year after 2026 run with the reverse charge off unless the adviser confirms an extension; the Reference Case keeps its setting. Both rulings are packaged as `Parameters.apply_bid_defaults()` (button "Apply the bid-scenario defaults (D119)") and surfaced as non-blocking `regulatory_warnings()` on the Parameters page. HIST-035: history rewritten on the CEO's word (`git filter-repo --replace-text`, 43 commits, tree of HEAD unchanged, tags moved, force-pushed 17.09.2026); v0.7.1 and v0.7.2 pushed | CEO rulings 17.09.2026 | TAR-2026, BRP-GF, RC-2027, HIST-035 closed |

