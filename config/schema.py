"""Schema for the configuration registers - placeholder (Phase 3).

Every parameter carries: key, value, unit, source, vintage, source_status
(verified | unverified | assumption), consumed (bool). Validation is added in Phase 3.
"""
